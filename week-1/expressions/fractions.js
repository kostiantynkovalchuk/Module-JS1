let fraction = "3/4";
let numerator;
let denominator;
// assign an expression to numerator to extract the numerator from the fraction variable
// assign an expression to denominator to extract the denominator from the fraction variable

let percentage;
console.log("percentage is ", percentage);
// log the value of percentage
